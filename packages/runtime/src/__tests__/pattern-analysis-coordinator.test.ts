import test from 'node:test'; import assert from 'node:assert/strict'; import { EvidenceQuery } from '@aes/kernel'; import { PatternAnalysisCoordinator } from '../pattern-analysis-coordinator.js'; import type { LearningEvidence } from '@aes/spec';
const row: LearningEvidence = { id: 'e1', traceId: 't1', signature: { taskClass: 'implementation', stage: 'execution', language: 'typescript' }, verification: 'passed', attributable: true, modelClass: 'balanced', retries: 0, userInterruptions: 0, providerRecoveries: 0, timestamp: '2026-08-09T00:00:00Z' };
test('pattern analyst failures isolate and healthy hypotheses become candidates only with evidence', async () => { const hypothesis = { id: 'h1', kind: 'model_preference' as const, applicability: { stage: 'execution' as const }, proposedEffect: { kind: 'model_preference' as const, prefer: 'balanced' as const }, evidenceQuery: { signature: { taskClass: 'implementation' }, minimumRefs: 1 } }; const throwing = new PatternAnalysisCoordinator({ analyzer: { analyze: async () => { throw new Error('unavailable'); } }, evidenceQuery: new EvidenceQuery(), maxCandidates: 3, maxAnalysisTokens: 3000 }); assert.deepEqual(await throwing.analyze([row]), []); const healthy = new PatternAnalysisCoordinator({ analyzer: { analyze: async () => [hypothesis] }, evidenceQuery: new EvidenceQuery(), maxCandidates: 3, maxAnalysisTokens: 3000 }); const result = await healthy.analyze([row], '2026-08-09T00:00:00Z'); assert.equal(result[0]?.status, 'candidate'); assert.deepEqual(result[0]?.evidenceRefs, ['e1']); });
