import test from 'node:test'; import assert from 'node:assert/strict'; import { EvidenceAcquisitionPolicy } from '../evidence-acquisition-policy.js';
test('evidence acquisition prefers natural, then replay, then controlled', () => { const policy = new EvidenceAcquisitionPolicy(); assert.equal(policy.choose({ naturalComparativeSufficient: true, replayAvailable: true, liveEligible: true }), 'natural'); assert.equal(policy.choose({ naturalComparativeSufficient: false, replayAvailable: true, liveEligible: true }), 'replay'); assert.equal(policy.choose({ naturalComparativeSufficient: false, replayAvailable: false, liveEligible: true }), 'controlled'); assert.equal(policy.choose({ naturalComparativeSufficient: false, replayAvailable: false, liveEligible: false }), 'unresolved'); });
