import test from 'node:test'; import assert from 'node:assert/strict'; import { EvidenceQuery } from '../evidence-query.js'; import type { LearningEvidence } from '@aes/spec';
const evidence = (id: string, modelClass: NonNullable<LearningEvidence['modelClass']>): LearningEvidence => ({ id, traceId: id, signature: { taskClass: 'implementation', stage: 'execution', language: 'typescript' }, verification: 'passed', attributable: true, modelClass, retries: 0, userInterruptions: 0, providerRecoveries: 0, timestamp: '2026-08-09T00:00:00Z' });
test('evidence query requires matching evidence and detects real alternatives', () => { const hypothesis = { id: 'h1', kind: 'model_preference' as const, applicability: { stage: 'execution' as const, language: 'typescript' }, evidenceQuery: { signature: { taskClass: 'implementation' }, minimumRefs: 2 } }; const insufficient = new EvidenceQuery().matchHypothesis(hypothesis, [evidence('e1', 'balanced')]); assert.equal(insufficient.eligible, false); assert.deepEqual(insufficient.evidenceRefs, ['e1']); const comparative = new EvidenceQuery().matchHypothesis(hypothesis, [evidence('e1', 'balanced'), evidence('e2', 'cheap')]); assert.equal(comparative.eligible, true); assert.equal(comparative.evidenceStrength, 'comparative'); });
