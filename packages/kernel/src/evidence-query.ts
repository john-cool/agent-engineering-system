import type { LearningEvidence } from '@aes/spec'; import type { PatternHypothesis } from '@aes/runtime-sdk'; import { matchesApplicability } from './task-signature.js';
export class EvidenceQuery { matchHypothesis(hypothesis: PatternHypothesis, evidence: readonly LearningEvidence[]) { const rows = evidence.filter((row) => row.attributable && matchesApplicability(row.signature, hypothesis.applicability)); const key = (row: LearningEvidence) => hypothesis.kind === 'model_preference' ? row.modelClass : hypothesis.kind === 'latency_preference' ? row.latencyMode : undefined; const alternatives = new Map<string, LearningEvidence[]>(); for (const row of rows) { const value = key(row); if (value) alternatives.set(value, [...(alternatives.get(value) ?? []), row]); } const comparative = alternatives.size >= 2; const controlled = comparative ? [...alternatives.values()].every((group) => group.some((row) => row.origin === 'controlled')) : rows.some((row) => row.origin === 'controlled'); return { eligible: rows.length >= hypothesis.evidenceQuery.minimumRefs, evidenceRefs: rows.map((row) => row.id).sort(), evidenceStrength: controlled ? 'controlled' as const : comparative ? 'comparative' as const : 'observational' as const }; } }
