export class ControlledEvaluationPolicy { constructor(private readonly policy: { minimumValueRatio: number }) {} decide(input: { expectedReuse: number; savingPerUse: number; actionableProbability: number; evaluationCost: number }): { run: boolean; expectedValue: number; ratio: number } { const expectedValue = input.expectedReuse * input.savingPerUse * input.actionableProbability; const ratio = input.evaluationCost <= 0 ? Number.POSITIVE_INFINITY : expectedValue / input.evaluationCost; return { run: ratio >= this.policy.minimumValueRatio, expectedValue, ratio }; } }
