# Model routing

Capability classes are `cheap`, `balanced`, and `powerful`. Hard quality and capability requirements are filtered first; soft latency and learned preferences rank acceptable models afterward.
