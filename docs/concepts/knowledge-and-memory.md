# Knowledge and memory

Typed records are `fact|decision|experience|preference` with session, project, or user scope, provenance, evidence references, lifecycle status, and relations. Retrieval is lexical and budgeted; indexes are derived and reproducible. Conflicts are surfaced, not silently resolved. Project-to-user promotion fails closed without privacy filtering and approval.
