# Resource governance

Budgets cover tokens, cost, retries, and duration. Unknown telemetry remains unknown and is never treated as zero. A hard deny requires the configured authority path; it cannot silently lower required quality.
