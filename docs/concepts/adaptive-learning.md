# Adaptive learning

`DecisionTrace -> LearningEvidence -> Candidate -> Shadow -> Evaluation -> Active Project Overlay -> New Work -> Regression Monitor -> keep/degrade`. Candidates are evidence-backed; shadow decisions never alter production; quality gates outrank savings.
