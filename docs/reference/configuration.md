# Configuration reference

Defaults: analysis candidates `3`, analysis tokens `3000`, incremental work `500ms`, evaluation samples `20`, comparable samples `5`, quality margin `0.01`, relative improvement `0.05`, regression window `20`, controlled runs `5`, controlled tokens/day `100000`, controlled cost/day `0.50`, retrieval records `8`, retrieval tokens `2500`, active records `500`, record tokens `800`, index tokens `4000`.
