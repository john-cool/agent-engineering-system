# Write an adapter

Implement `RuntimeProvider` and `RuntimeSession` from `@aes/runtime-sdk`, normalize provider events, preserve ambiguous side-effect boundaries, and keep vendor types outside kernel/runtime-sdk.
