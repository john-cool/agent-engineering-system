# Authority promotion example

Fifteen approvals, zero rejections, and fifteen verified successes create an `AuthorityCandidate`. Explicit user approval separately creates a scoped `ScopedAuthorityGrant`. Verified regression may change autonomous to assisted; it never promotes assisted back automatically.
