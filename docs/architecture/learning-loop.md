# Learning loop

`DecisionTrace -> LearningEvidence -> Candidate -> Shadow -> Evaluation -> Active Project Overlay -> New Work -> Regression Monitor -> keep/degrade`. Authority and knowledge promotion are separate explicit paths.
