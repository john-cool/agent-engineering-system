# Codex adapter

`@aes/adapter-codex` maps Codex App Server messages into neutral events and runs one provider process per workspace. Raw provider capture is opt-in and sanitized fixtures are used offline.
