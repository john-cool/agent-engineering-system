# Adaptive runtime

Adaptive Runtime resolves a model, enforces resources and control, supervises one provider per workspace, recovers safely, persists a normalized trace, and optionally notifies learning after persistence. Learning failure cannot fail a successful task.
