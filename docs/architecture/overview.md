# Architecture overview

`@aes/spec` defines contracts; `@aes/kernel` makes deterministic decisions; `@aes/runtime-sdk` defines provider and storage boundaries; `@aes/runtime` orchestrates execution and learning side effects; adapters remain at the edge.
